# 变更记录：全量代码学习（codebase onboarding）

| 字段 | 内容 |
|------|------|
| **编号** | `20260807-codebase-full-learning` |
| **日期** | 2026-08-07 |
| **状态** | **已验收** |
| **负责人** | Codex Agent（Superpowers 全量学习） |
| **关联** | 本次会话；产出 `docs/codebase-learning.md` |

---

## 0. 用户价值（必填 · 站在用户角度）

- **谁用：** 后续所有接手本仓库的协作者 / Agent / 开发者。
- **解决什么痛点：** 新协作者面对 16 个 crate + Flutter + 脚本，不知道产品共识、架构分层、数据流与文档真实度，容易走错路径或踩「文档声称已实现」的坑。
- **用完后用户多得到什么：** 一份带文件/行号引用的全量学习地图 + 30 条已核实的差异/死代码/未接线清单，后续每次改动可先查报告再动工，避免重复全仓扫描；产品演进（P0「第二次更准」）的工程侧即「协作者更快接对线」。
- **好用性自检：**
  - [x] 不需要额外运行时 / 数据库（纯文档）
  - [x] 步骤可感知、可预期（6 个子代理并行深读 + 抽查）
  - [x] 不增加无意义确认或噪音
  - [x] 高频路径（找 crate / 找入口 / 找差异）比改前更快

---

## 0b. 产品经理视角（必填 · 禁止跳过）

- **场景：** 仓库持续演进，新会话/新协作者需要快速建立正确心智模型。
- **路径变化：** 改前：逐 crate 重读、靠 project-map 但地图已过期（2026-08-06，P0 v0.3 引用）；改后：先读 `docs/codebase-learning.md`（含权威文档层级、架构速记、数据流、问题清单），再按需精读具体 crate。
- **成功标准：** 报告覆盖全部 16 个 crate + Flutter + scripts；关键断言（companion 协议、防白屏、token 鉴权、auto_accept 默认、BM25 缺失）经抽查与代码一致；发现的问题清单可被后续变更引用。
- **明确不做什么：** 本次不修复任何代码；不改变产品定义；不动 P0/P1 权威文档；不把发现的问题直接当「已处理」。

---

## 0c. 架构师视角（必填 · 禁止修修补补）

- **根因层级：** 知识管理 / 协作接续层——问题是「新协作者无法低成本获得准确全貌」，而非某个功能缺陷。
- **正确的长期默认路径：** 权威文档（P0/P1/P2/P3）为唯一共识源；`docs/` 存放非权威说明；学习快照入 `docs/` 并登记索引；问题清单与台账绑定，后续修复各自走 `docs/records/` 四阶段。
- **与引擎/各入口边界：** 纯文档变更，不触及任何 crate；报告如实记录「server 1:1 不成立」「三份平行 ContextSources」等与 AGENTS 原则相悖的现状，供后续架构债处理引用。
- **安全影响：** 无代码变更；报告仅复述已公开的安全设计（token 永不裸奔、0600、路径校验），未泄露任何密钥。
- **如何防复发：** `docs/README.md` 索引新增本报告；报告文末注明「随代码演进可能过时，改动后同步更新或归档」。
- **为何这不是补丁：** 这是「先建立正确心智再动工」的根因路径，不是为某个症状打的临时补丁。

---

## 1. 方案（Plan）

- **目标：** 全量学习仓库（产品共识 + 16 crate + Flutter + scripts），产出可引用的学习报告与台账。
- **范围：** 做：只读深读全部子系统、汇总差异/死代码/未接线清单、写 `docs/codebase-learning.md` + 台账 + 更新两份索引。**不做**：任何代码修改、权威文档升版、问题修复。
- **用户路径变化：** 无（内部协作文档）。
- **技术要点：** 精读 P0/P1/P2/P3 → 6 个并行只读子代理（引擎核心 / 工具技能 / 记忆进化 / CLI / GUI·server·channel / IM·Flutter·脚本）→ 关键断言抽查（companion_protocol、tauri 无 devUrl、BM25 缺失、auto_accept 默认、token 来源、run-gui.sh）→ 汇总落盘。
- **风险与回滚：** 报告可能随代码演进过时 → 文末明示；回滚 = 删除报告与台账行。
- **方案确认：** [x] 已对照 P0/P1（含第七条）· 2026-08-07 · Codex Agent

---

## 2. 实施（Implement）

- **实际改动摘要：** 只读学习 + 新增 `docs/codebase-learning.md`（176 行）+ 本台账 + `docs/README.md` 索引一行 + `docs/records/README.md` 索引一行。无代码改动。
- **关键路径/文件：**
  - 新增：`docs/codebase-learning.md`
  - 新增：`docs/records/20260807-codebase-full-learning.md`（本文件）
  - 更新：`docs/README.md`（结构表加一行）
  - 更新：`docs/records/README.md`（索引顶部加一行）
- **偏离方案处：** 无。

---

## 3. 测试（Test）

| # | 用例（用户语言） | 步骤 | 期望 | 结果 | 备注 |
|---|------------------|------|------|------|------|
| 1 | 报告覆盖所有子系统 | 对照 `Cargo.toml` members 与 `clients/`、`scripts/` 检查报告章节 | 16 crate + Flutter + scripts 全覆盖 | 通过 | §4.1–4.6 |
| 2 | 关键断言与代码一致 | `rg` 抽查 companion_protocol:28 / tauri frontendDist / BM25 零命中 / auto_accept=false / server 路由 / run-gui.sh | 全部一致 | 通过 | 抽查命令见会话 |
| 3 | 根目录 md 白名单未破坏 | `ls *.md` | 仍仅 4 份权威 | 通过 | 新文档在 `docs/` |
| 4 | 索引可导航 | 打开 `docs/README.md`、`docs/records/README.md` | 新行可达 | 通过 | — |

- **自动化：** 本次无代码变更，不适用 `cargo test` / `clippy`（后续修复问题清单中的条目时再按门槛执行）。
- **手工：** 无 GUI/手机/IM 场景（纯内部学习文档）。
- **测试结论：** [x] 全部通过 · [ ] 有已知问题（列出）

---

## 4. 验收（Accept）

| 门槛 | 是否达标 | 说明 |
|------|----------|------|
| 用户价值成立 | ☑ | 后续协作者可先读报告再动工，避免重复扫描与走错路 |
| 开箱即用未破坏 | ☑ | 未触碰任何运行路径 |
| 本地优先未破坏 | ☑ | 纯文档，无数据/密钥变更 |
| 测试通过 | ☑ | 4 项文档用例全过；无代码测试适用 |
| 记录完整 | ☑ | 本台账四阶段写全 |
| 产品+架构两视角齐全 | ☑ | §0b/§0c |
| 非修修补补（默认路径正确） | ☑ | 学习快照 + 台账，是协作接续的正确默认路径 |
| 代码卫生：高效无冗余、旧代码/注释/入口已清理（P0 第九条） | ☑ | 本次未改代码；报告如实列出 30 条待清理项供后续变更引用（不在此次越权清理） |

- **验收人：** Codex Agent
- **验收日期：** 2026-08-07
- **结论：** ☑ 通过 · ☐ 驳回
- **遗留项：** 问题清单 A/B/C（30 条）列为后续变更候选，各自单独走 `docs/records/` 四阶段，不在本次处理。

---

## 5. 附注

- 学习过程：6 个只读子代理（hermes-core/llm/turn/mcp；tools/skills；store/memory/reflect；cli；gui/server/channel；weixin/feishu/telegram+flutter+scripts）并行深读，主代理抽查 6 项关键断言后汇总。
- 已知文档版本漂移：`docs/project-map.md`（2026-08-06，引 P0 v0.3）落后于当前 P0 v0.5 / P1 v0.1；本次不改 project-map，已在问题清单与报告 §7 备忘。
