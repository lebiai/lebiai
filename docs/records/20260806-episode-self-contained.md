# 变更记录：工作情节必须自包含（禁「见会话记录」/ Care 污染）

| 字段 | 内容 |
|------|------|
| **编号** | `20260806-episode-self-contained` |
| **日期** | 2026-08-06 |
| **状态** | **已实施** |
| **触发** | 用户指出兜底情节把 `[Hermes Care]` 当内容，且「见会话记录」删会话后无用 |

## 问题

1. 会话结束兜底把 **Care 注入的 User 文本**当成用户意图  
2. 模板写 **见会话记录** → 删 JSONL 后记忆无信息量  
3. 宁要空候选，不要污染记忆库  

## 产品规则（钉死）

**工作情节在会话删除后仍须可读、可检索。**  
禁止：见会话记录、内部 nudge、空壳模板。

## 实施

- `episode_is_self_contained` / `is_internal_noise_text`  
- finalize 丢弃空洞情节；种子只来自**有实质的 summary**  
- GUI fallback 只抽真实人类话；不够则 **零候选**  
- reflection 提示词禁止会话指针  

## 测试

- [x] hollow + Care nudge 情节被丢弃  
- [x] 好 summary 种子不含「见会话记录」  
