# 变更记录：历史会话不露内部指令 · 过程收成一条

| 字段 | 内容 |
|------|------|
| **编号** | `20260814-history-care-and-process` |
| **日期** | 2026-08-14 |
| **状态** | 实施中 |
| **种类** | F 台账 · 产品 |
| **关联** | 历史会话里出现 `[lebi-AI Care]`；过程条下复制 |

---

## 0-fp

- **拒绝的类比：** 不是再加一个开关藏气泡。
- **拆出的真：** Care 是引擎给模型的下一句，不是用户说的话，不该落盘、不该画成用户气泡。一次提问只该看到一折过程 + 一句答，复制只复制答。
- **如何推出：** 落盘与回放都丢掉内部指令；同一问下的多段助手过程合成一条。

## 0 / 0b / 0c

用户打开旧对话不该看见英文内部指令，也不该对着一串搜搜搜每条点复制。

## 1. 方案

做：过滤 Care、合并过程。第 3 条（无变化不重蒸）只出方案，本记录不实施。

## 2. 实施

- `is_internal_instruction_text` + `for_persist` 空内容 + sanitize 丢掉
- 画布：不渲染该用户气泡；同一用户问下的助手过程合成一条，只留最后一段正文
