# 变更记录：v1.1.0 发布（在办/回顾/授权 + 安装包挂 GitHub）

| 字段 | 内容 |
|------|------|
| **编号** | `20260818-release-v1.1` |
| **日期** | 2026-08-18 |
| **状态** | **已验收**（Release 正文 + dmg + exe 已挂上）
| **型** | 发布 |
| **关联** | [`20260809-release-v1.0`](./20260809-release-v1.0.md) · [`../releases/release-notes.md`](../releases/release-notes.md) |

---

## 0-fp. 第一性原理

- **拒绝的类比：** 不是另起一套发版流程；不是用 1.0.0 覆盖线上包。
- **拆出的真：** 1.0 之后用户能感到在办、回顾、试用授权；版本号必须新；CI 已有 tag → dmg/exe → Release。
- **如何推出：** 升 1.1.0，改发布说明，推 main + `v1.1.0`。签发私钥不进 git。

## 0. 用户价值

GitHub Releases 可下 1.1 的 dmg / exe，说明写的是这一版真有的能力。

## 0b–0c

沿用既有 `release.yml`。`body_path` 必须是 `docs/releases/release-notes.md`。

## 1. 方案

版本三处对齐；重写 1.1 说明；安装指引 Windows 转换器改为已捆绑；提交工作区（不含 `issue-license.py` / `license-issuer.html`）；推 main；打 tag。

## 2–4

见实施与 CI。
