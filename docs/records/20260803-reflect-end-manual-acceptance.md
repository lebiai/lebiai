# 变更记录：REFLECT-END 真机手测验收（会话结束自动提炼）— 待验收

| 字段 | 内容 |
|------|------|
| **编号** | `20260803-reflect-end-manual-acceptance` |
| **日期** | 2026-08-03 |
| **状态** | **待验收** |
| **负责人** | Codex（用户委托）→ 真机手测需用户执行 |
| **关联** | `20260803-reflect-end-session-reflection`（实现记录，状态已降级为待验收） |

---

## 0. 用户价值（必填 · 站在用户角度）

- **谁用：** 个人用户 / 开发者，CLI `hermes chat` 主路径
- **解决什么痛点：** 「会话结束自动提炼」是 P0/README 承诺的核心价值，代码已接线，
  但**真机端到端行为尚未验证**——没跑过真实 LLM 会话，不能宣称完成
- **用完后用户多得到什么：** 确认「退出即提炼、候选可确认/拒绝」在真实环境成立；
  验收通过后 REFLECT-END 才算真正完成
- **好用性自检：**
  - [x] 不需要额外运行时 / 数据库
  - [x] 步骤可感知、可预期（下方用例即验收步骤）
  - [x] 不增加无意义确认或噪音（只验证默认路径）
  - [x] 高频路径比改前更快或更省心（验收通过即兑现承诺）

---

## 0b. 产品经理视角（必填 · 禁止跳过）

- **场景：** 用户用真实 API Key 聊完一轮会话后退出
- **路径变化：** 待验证目标：≥3 轮用户输入后 `/exit` 或 Ctrl-D → 自动出现
  「reflecting on session...」→ 候选逐条确认/拒绝 → 确认项落盘明文
- **成功标准：** 见第 3 节用例表（1–5 全部通过）
- **明确不做什么：** 不改任何代码（本记录只验收）；不验证 `/reflect` 手动路径（已有实现）

---

## 0c. 架构师视角（必填 · 禁止修修补补）

- **根因层级：** 验收层（实现已完成，端到端证据缺失）
- **正确的长期默认路径：** 验收 = 用户语言用例全部通过 + 落盘文件可检视；
  通过后由验收人在本记录 4 节打勾并升状态为「已验收」，同步原实现记录
- **与引擎/各入口边界：** 只验收 `hermes-cli` chat 退出路径；GUI/server 的手动
  reflection 不在本记录范围
- **安全影响：** 无（真机手测需用户自备 API Key，仅在用户本机执行）
- **如何防复发：** 验收结果写入本记录与 reflect-log；若失败，回 `20260803-reflect-end-session-reflection`
  走修复流程（新记录，不叠补丁）
- **为何这不是补丁：** 这是验收动作本身，是「未验收 = 未完成」规则的执行

---

## 1. 方案（Plan）

- **目标：** 完成 REFLECT-END 真机端到端验收
- **范围：** 做：用户按用例手测并记录结果。**不做：** 代码改动；GUI/server 路径
- **前置条件：** `~/.small-rust-hermes/config.toml` 已配置可用 API Key；`cargo build -p hermes-cli`
  或使用已构建的 release 二进制
- **风险与回滚：** 手测消耗少量 API 额度；无代码风险
- **方案确认：** [x] 已对照 P0/P1（含第七条）· 日期/人：2026-08-03 / Codex

---

## 2. 实施（Implement）

- **实际改动摘要：** 无（验收记录；实现见 `20260803-reflect-end-session-reflection`）
- **关键路径/文件：** 执行 `hermes chat`
- **偏离方案处：** 无

---

## 3. 测试（Test）— 用户手测步骤

| # | 用例（用户语言） | 步骤 | 期望 | 结果 | 备注 |
|---|------------------|------|------|------|------|
| 1 | 退出自动提炼 | `hermes chat` 聊 ≥3 轮（含一次「记住…」），然后 `/exit` | 出现「reflecting on session...」，候选逐条提示确认/拒绝 | ⬜ 未执行 | 需要 API Key |
| 2 | 确认落盘 | 对 1 个 skill/memory 候选选确认（y） | `~/.small-rust-hermes/skills/` 或 `memories/` 出现对应 Markdown | ⬜ 未执行 | 明文可读 |
| 3 | 拒绝不落盘 | 对 1 个候选选拒绝（n） | 无新文件；reflect-log.jsonl 记 Rejected | ⬜ 未执行 | |
| 4 | 短会话静默 | 聊 1 轮后退出 | 无「reflecting」输出（< min_turns=3 静默跳过） | ⬜ 未执行 | |
| 5 | 管道不卡死 | `printf 'hi\n' \| hermes chat`（无 API 交互）后 EOF | 不阻塞、正常退出；无半成品写入 | ⬜ 未执行 | stdin 关闭路径 |

- **自动化：** 已在实现记录中全绿（check/test/clippy）；本记录只做端到端手测
- **测试结论：** [ ] 全部通过 · [ ] 有已知问题（列出）——**当前：未执行，待验收**

---

## 4. 验收（Accept）

对照 **质量门槛**（见仓库根 `DEVELOPMENT_RULES.md` §变更流程）：

| 门槛 | 是否达标 | 说明 |
|------|----------|------|
| 用户价值成立 | ⬜ 待手测 | 用例 1–3 通过后成立 |
| 开箱即用未破坏 | ⬜ 待手测 | 用例 5 覆盖 |
| 本地优先未破坏 | ⬜ 待手测 | 落盘位置与明文格式 |
| 测试通过 | ⬜ 待手测 | 用例 1–5 |
| 记录完整 | ✅ | 本记录四阶段齐全 |
| 产品+架构两视角齐全 | ✅ | 见 0b/0c |
| 非修修补补（默认路径正确） | ✅ | 验收动作本身 |
| 代码卫生（P0 第九条） | ✅ | 无代码改动 |

- **验收人：** 待用户（真机手测后填写）
- **验收日期：** 待定
- **结论：** ☐ 通过 · ☐ 驳回（原因：）——**当前：待验收**
- **遗留项：** 无（验收通过后回写 `20260803-reflect-end-session-reflection` 为已验收）

---

## 5. 附注

- 前置：`~/.small-rust-hermes/config.toml` 需可用的 `default_provider` + API Key
- 手测注意：`auto_accept_memories` 默认已为 `false`，候选应逐条询问，不会静默写入
