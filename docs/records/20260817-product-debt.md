# 变更记录：收口产品债（在办 v2 冻结 · 快照刷新 · 目视不代签）

| 字段 | 内容 |
|------|------|
| **编号** | `20260817-product-debt` |
| **日期** | 2026-08-17（工程验收 2026-08-18） |
| **状态** | **工程已验收** · 桌面目视仍待用户 |
| **型** | 产品 |
| **关联** | [`../spec/zaiban-work.md`](../spec/zaiban-work.md) · [`20260817-zaiban-work-unify`](./20260817-zaiban-work-unify.md) · [`20260817-review-ledger`](./20260817-review-ledger.md) |

---

## 0-fp. 第一性原理

- **拒绝的类比：** 不是把「待产品确认」批量改成已验收。不是把在办扩到 Flutter / IM 来填能力矩阵。不是再写一份第二宪法。
- **拆出的真：**
  1. 产品债里能收的，是规格与代码不一致、目录撒谎、空态/待收下/已蒸过走不通。
  2. 用户没点头的目视，代理人签了 = 撒谎。
  3. 在办 v1 只在桌面 GUI，是已拍板，不是缺口。
- **如何推出：** 冻结 v2 已写清的结论；补齐对照规格仍缺的操作；刷新快照；8/14 工程已齐项保持待目视。

## 0. 用户价值

记下要日子、过了换期、空态能选日子、待收下能收下、回顾看得出这段蒸过。地图不再把人带到两周前。

## 0b. 产品经理

- **场景：** 桌面 GUI · 对话右上角在办抽屉。
- **怎么走完：** 空态见芯片 → 记下一句+日子；待收下先选日子再收下；过了点还做再换期；回顾目录若已蒸，写「这段已经回顾过」，点进去读账。
- **看起来：** 主色蓝；改/做完/丢掉常驻；过了用琥珀，不红。
- **成功标准：** 规格 §11 工程项可走通。目视仍是用户的。
- **不做什么：** 代签 8/14；给 Flutter / IM 写在办；系统通知。

## 0c. 架构师

- **根因：** 规格 §10 过时；Cue 收下/新建没带期限；空态没露出芯片；回顾目录不管有没有账都是同一颗主按钮。
- **默认路径：** `commitments.json` 无期不成债；Cue 与抽屉同一套 DueChips；回顾目录读 `list_reviews`。
- **边界：** 只动 GUI + 规格 + 快照。Server / Flutter / IM 不扩。
- **为何不是补丁：** 按已写清的 v2 结论收口，不是再叠一条特例。

---

## 1. 方案

- 拍板规格 10–12（周报底稿 · 无期限不成债 · 改删常驻），升 v2.0。
- 补：空态芯片、Cue 收下/新建要日子、目录「已蒸过」、过了当天只问一次。
- 清 ZaibanBlock 未用的 compact。
- 刷新 `docs/snapshot/project-map.md`。
- 8/14「待产品确认」不改成已验收。

## 2. 实施

- 规格 `zaiban-work.md` 升 **v2.0**，条款 10–13 标已拍。§10 对照改成与代码一致。
- 空态露出期限芯片；点芯片即进入「记」。
- 对话 Cue：待收下无期要先选日子；近义「仍新建」要日子；过了当天只问一次（本机记下问过的 id）。
- 回顾目录：这段已蒸过先写出来，主按钮改「看交差账」，「再看一次」退后。
- 去掉 ZaibanBlock 未用的 compact。
- 单测：`accept_suggested` 无期拒绝。
- 刷新 `docs/snapshot/project-map.md`（2026-08-17）。
- **未做：** 不把 8/14「待产品确认」改成已验收；不给 Flutter / IM 写在办。

**偏离方案处：** 无。

## 3. 测试

| # | 用例（用户语言） | 步骤 | 期望 | 结果 |
|---|------------------|------|------|------|
| 1 | 尽快记不下 | `parse_due("尽快")` | Vague | 通过 |
| 2 | 无期不成债 | `save` 开着的债无日期 | MissingDue | 通过 |
| 3 | 改标题留期限 | `retitle` | 日期不变 | 通过 |
| 4 | 待收下无期拒收 | `accept_suggested` 无日期 | MissingDue | 通过 |
| 5 | 回顾不要脚本 | `is_content_deliverable("*.py")` | false | 通过 |
| 6 | GUI 能编过 | `tsc` + `npm run build` | dist 更新 | 通过 |
| 7 | 全仓测试 | `cargo test --workspace` | 全绿 | 通过（2026-08-18） |
| 8 | 全仓 clippy | `cargo clippy --workspace --all-targets -- -D warnings` | 全绿 | 通过（2026-08-18；顺手修 MSRV / tickets） |

### 规格 §11 工程对照（2026-08-18）

| 用户语言 | 代码路径 | 工程 |
|----------|----------|------|
| 记下必须有日子；尽快记不下 | `due.rs` + `save` + 空态/Cue 芯片 | 齐 |
| 每条看得见期限；过了排最前写「过了」 | `dueLabel` 琥珀 + `rank_due` | 齐 |
| 过了问还做 → 还做必须换期 | Cue `pendingRedue` + DueChips | 齐 |
| 不悬停也能改/做完/丢掉 | `ZaibanBlock` 常驻 RowBtn | 齐 |
| 改标题后期限还在 | `retitle` 只改 title | 齐 |
| 回顾没有 py | `is_content_deliverable` | 齐 |
| 读账能收起；目录与正文不挤 | `ReviewPane` 独占读账 + 收起 | 齐 |
| 无通知/日历/循环/不默认周日 | 无相关代码；节奏默认 0 | 齐 |
| 目视走抽屉 | Tauri 窗口 | **未点** |

- **手工：** 本环境不能点桌面窗口。请 `scripts/run-gui.sh` 走 §11 最后一条。

## 4. 验收

| 门槛 | 是否达标 | 说明 |
|------|----------|------|
| 用户价值成立 | 是 | 记下有日子、过了换期、已蒸过能看见 |
| 开箱即用未破坏 | 是 | 无新运行时 |
| 本地优先未破坏 | 是 | 仍明文文件 |
| 测试通过 | 是 | workspace test + clippy `-D warnings` 2026-08-18 全绿 |
| 记录完整 | 是 | 本文 |
| 产品+架构两视角齐全 | 是 | |
| 非修修补补 | 是 | 按已写清的 v2 结论收口 |
| 代码卫生 | 是 | 清 compact；clippy 预存 MSRV / tickets 一并清 |
| 操作与视觉 | 工程对照齐 | **窗口未点，不代签目视** |
| 第一性原理 | 是 | 不代签、不扩入口 |

- **验收人：** agent（工程）
- **验收日期：** 2026-08-18
- **结论：** 工程通过。目视未勾 = **不得对外声称 v2 已交付**。
- **遗留：** ZAIBAN-EYE（你走一遍抽屉）；AUG14-EYE（8/14 目视，不代签）。
