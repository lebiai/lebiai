# 变更记录：权威唯一 · 禁止偏移

| 字段 | 内容 |
|------|------|
| **编号** | `20260814-authority-no-drift` |
| **日期** | 2026-08-14 |
| **状态** | 工程通过 · 待产品确认 |
| **负责人** | Agent |
| **关联** | P0 升 v0.7；P1 升 v0.2；身份协议与 docs 收束 |

---

## 0. 用户价值（必填 · 站在用户角度）

- **谁用：** 个人用户（读 README / 用 GUI）；协作者与 Agent（读 P0/P1/AGENTS 后改产品）
- **解决什么痛点：** 权威层版本互不对齐、许可证与入口角色未写入宪法、docs 另立叙事，导致实现和文案漂离「工作搭子」
- **用完后用户多得到什么：** 打开产品看到的路径、授权、关系用词与仓库最高约束一致；不会被过期地图或探索方案带偏
- **好用性自检：**
  - [x] 不需要额外运行时 / 数据库
  - [x] 步骤可感知、可预期
  - [x] 不增加无意义确认或噪音
  - [x] 高频路径比改前更快或更省心（协作者先读一处法即可）

---

## 0b. 产品经理视角（必填 · 禁止跳过）

- **场景：** 协作者/Agent 改功能或写文档时，不知道以哪份 md 为准；用户读 README 以为手机/IM「正在开发」，或不知道有试用
- **路径变化：** 改前：P0 v0.6、AGENTS 仍写 v0.5、P1 默认英文、CLI/GUI 双主、license 只在 specs、探索方案与蓝图平级。改后：P0 是唯一产品法；用户默认路径=GUI；授权是产品事实；下游必须分级并声明服从 P0
- **成功标准：**
  1. 根目录四份权威文版本对齐 P0 v0.7
  2. 用户可见主词仍是「对话」「搭子」；`companion.rs` 不再写「工作伴侣 / partner 当搭档」
  3. `docs/` 索引把探索/快照/规格分开；`nl-admin-control-plane` 标「探索·未立项」
  4. README 诚实：授权一句、入口状态一句、隐私不说满
- **明确不做什么：** 不改授权业务规则；不立项控制平面；不重写全部台账历史；不把 CLI 从引擎入口里拿掉

---

## 0c. 架构师视角（必填 · 禁止修修补补）

- **根因层级：** 文档权威 / 身份协议 / 入口契约（不是 UI 表象）
- **正确的长期默认路径：** 产品定义、入口角色、授权、用词只在 P0；P1 执行、AGENTS 入口、README 用户子集；`companion.rs` 是身份协议唯一源码且必须与 P0 同词；`docs/` 全是下游
- **与引擎/各入口边界：** 共享 `hermes-core::companion`；server 仍是 GUI 的 Flutter 子集（能力矩阵，禁止 1:1）
- **安全影响：** 不改 token / TLS / 0600；只把已落地的授权与 IM fail-closed 写进宪法
- **如何防复发：** P0 第十条强制下游分级与升版同步；AGENTS 自检禁止回写蓝图当第二宪法
- **为何这不是补丁：** 一次把「法 / 执行 / 身份源码 / 下游分级」收到单一默认路径，不是给某篇过期文档加脚注

---

## 1. 方案（Plan）

- **目标：** 权威唯一，下游不得偏移
- **范围：** P0/P1/AGENTS/README；`companion.rs`；onboarding 种子用词；docs 索引与现行解释/规格/快照/探索文首
- **不做：** 重写历史台账正文；实施 nl-admin；改许可证状态机
- **方案确认：** [x] 已对照 P0/P1（含第七条）· 2026-08-14

---

## 2. 实施（Implement）

- P0 升 **v0.7**：定义卡补入口/授权/用词；新增第十条「权威唯一、禁止偏移」；第六条下游分级；清单 14–16
- P1 升 **v0.2**：默认语言 zh-CN；四层技能；GUI=用户默认 / CLI=引擎入口；避坑 14–16
- AGENTS / README 对齐；README 诚实写试用与入口状态
- `companion.rs` / `companion_context.rs` / onboarding 种子与 P0 同词（搭子 / work companion）
- `docs/` 索引按解释/规格/快照/探索/台账分级；`nl-admin-control-plane` 降为探索·未立项
- 偏离方案处：无。历史台账正文不改写（台账 ≠ 现行法）

---

## 3. 测试（Test）

| # | 用例（用户语言） | 步骤 | 期望 | 结果 | 备注 |
|---|------------------|------|------|------|------|
| 1 | 权威层对齐 | 读四份根文档文首 | 均指向 P0 v0.7 / P1 v0.2 | 通过 | |
| 2 | 身份协议同词 | `rg` companion.rs | 有搭子/work companion，无工作伴侣 | 通过 | |
| 3 | 引擎单测 | `cargo test -p hermes-core companion` | 6 passed | 通过 | |
| 4 | 引导种子 | `cargo test -p hermes-gui parses_name_with_scenario` | 通过 | 通过 | |

- **自动化：** `cargo test -p hermes-core companion`（6 passed）；`cargo test -p hermes-gui parses_name_with_scenario`（1 passed）
- **手工：** `docs/README.md` 已按分级重排
- **测试结论：** [x] 全部通过 · [ ] 有已知问题（列出）

---

## 4. 验收（Accept）

| 门槛 | 是否达标 | 说明 |
|------|----------|------|
| 用户价值成立 | ☑ | 协作者与用户不再被双主/过期法带偏 |
| 开箱即用未破坏 | ☑ | 未引入运行时 |
| 本地优先未破坏 | ☑ | 授权仍是本地验签 |
| 测试通过 | ☑ | companion 6 + onboarding 1 |
| 记录完整 | ☑ | 本文 |
| 产品+架构两视角齐全 | ☑ | |
| 非修修补补（默认路径正确） | ☑ | 单一产品法 + 下游分级 |
| 代码卫生：高效无冗余、旧代码/注释/入口已清理（P0 第九条） | ☑ | 身份用词旧路径已改 |

- **验收人：** 待产品确认
- **验收日期：**
- **结论：** ☐ 通过 · ☐ 驳回
- **遗留项：** 历史台账里的「搭档/伴侣」不改写（台账是当时记录，不构成现行法）
