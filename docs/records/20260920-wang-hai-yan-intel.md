# 变更记录：重写情报王海燕

| 字段 | 内容 |
|------|------|
| **编号** | `20260920-wang-hai-yan-intel` |
| **日期** | 2026-09-20 |
| **状态** | **待验收**（人物定义已换；桌面目视一轮采集） |
| **负责人** | |
| **关联** | 用户给出采集执行方案：全量、时间校验、交叉验证、完整概要、A/B/C、固定三栏、允许名单 |

---

## 0-fp. 第一性原理

- **拒绝的类比：** 不是「写 30 条标题摘要的聊天」；也不是假装能把 Wind 终端和 50 个官网在一轮里爬干的爬虫农场。
- **拆出的真：** 吕老师选题要的是：窗口内、名单内、带发布时间与原文链接、事件写全、标了 A/B/C 的料。旧帽子既不守时间，也不守格式，下游没法研判。
- **如何从真推出：** 帽子重写为这份执行方案。全量 = 窗口内名单上本轮拿得到的全部有效条，不设条数上限；拿不到的站点必须【站点异常】；输出只能是宏观 / 产业动态 / 资本市场三栏。

---

## 0. 用户价值

- **谁用：** 点王海燕，或财富早知道第一棒。
- **痛点：** 极简概述、30 条截断、旧闻混入、无出处、无分级。
- **用完后：** 一份固定格式的资讯卷，吕老师能直接选。
- **好用性自检：** 不新加运行时；步骤仍是开口即采；失败可见。

---

## 0b. 产品经理视角

- **场景：** 用户说开工或「今天的资讯」。
- **怎么走完：** 确认窗口（默认当日北京时间，可切近 3 天）→ 按名单抓 → 时间过滤 → A 级交叉核 → 分级 → 三栏落盘 → 文末统计。
- **看起来怎么样：** 对话里摘要；文件在 `outputs/<日期>/资讯-<日期>.md`。缺类写「本周期无匹配资讯」。站点失败写【站点异常】。
- **成功标准：** 定义里不再有 30 条、五栏旧分类、只写标题。
- **不做：** 不假装能登录 Wind；不改流水线交棒判据（仍是非选题文件）；本轮不做抓取域名硬拦（帽子先立住）。

---

## 0c. 架构师视角

- **根因：** 职责只是提示词上的采访纪律，且已经过时。
- **默认路径：** 人物定义是这顶帽子的真源；名单与格式跟帽子走，不另做全局技能（免得吕老师技能索引里出现采集手册）。
- **边界：** 仍用 `web_search` / `web_fetch` / `write`。全量受工具轮次与反爬限制，用【站点异常】暴露，不静默。
- **防复发：** 正文禁止「单日不超过 30 条」；输出结构写死三栏。
- **为何不是补丁：** 换的是职责本身，不是再加一句「请写全一点」。

---

## 1. 方案

- **做：** 重写 `wang-hai-yan.md`；组里那一摊活的文案对齐；规格 §3 产物对齐。
- **不做：** 域名硬拦；改交棒；Wind 适配器。
- **方案确认：** [x] 2026-09-20

---

## 2. 实施

- **实际改动摘要：** `wang-hai-yan.md` 换成全量 / 时间窗 / 名单 / A/B/C / 三栏交卷；组里 duty 与规格 §3 对齐。
- **偏离方案处：** 本轮未做抓取域名硬拦（帽子先立住）。Wind 登录墙按【站点异常】报，不接终端。

---

## 3. 测试

| # | 用例 | 期望 |
|---|------|------|
| 1 | 读帽子 | 无 30 条上限；有三栏、A/B/C、允许名单、【站点异常】 |
| 2 | 人物仍解析 | parse + 指路不指向已下线 |

---

## 4. 验收

待目视一轮采集。遗留：名单外 URL 的引擎硬拦另开。
