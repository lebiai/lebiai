# 变更记录：v1.3.0 发布（材料 / 在办顺滑 / 手册 / UTF-8 / 可点更新）

| 字段 | 内容 |
|------|------|
| **编号** | `20260818-release-v1.3` |
| **日期** | 2026-08-18 |
| **状态** | **已验收**（Release 正文 + dmg + exe + latest.json 已挂上） |
| **型** | 发布 |
| **关联** | [`20260818-full-audit-six`](./20260818-full-audit-six.md) · [`20260818-remaining-closeout`](./20260818-remaining-closeout.md) · [`../releases/release-notes.md`](../releases/release-notes.md) |

---

## 0-fp. 第一性原理

- **拒绝的类比：** 不是把 8/18 批次塞进旧版本号；不是在说明里写没实现的能力。
- **拆出的真：** e8165c8 已带材料、分区归一、在办目视修复、手册、UTF-8 修复；1.2 的安装包已带更新器；用户从 1.2 点「更新」需要一个新版本号 + latest.json 指向新 tag。
- **如何推出：** 升 1.3.0（三处对齐 + lockfile）；发布说明只写本版真有；推 main + `v1.3.0`，由 `release.yml` 构建 dmg/exe 并挂 GitHub Release。

## 0. 用户价值

GitHub 可下 1.3 的 dmg / exe；1.2 用户可在设置里点「更新」升级，不再手动找安装包。

## 0b–0c

沿用 `release.yml`（先跑 `check-updater-secrets.sh` 验签密钥注入）。`body_path` 必须是 `docs/releases/release-notes.md`。私钥不进 git，走 Actions secrets。

## 1. 方案

版本三处对齐 1.3.0；重写 1.3 发布说明；`in-app-update` 规格状态改为「1.3 起可点更新」；写本台账；提交推 main；打 `v1.3.0`。

## 2. 实施

见本次提交与 tag。

## 3. 测试

- `cargo check --workspace` 通过（lockfile 同步 1.3.0）。
- 发布 CI：macOS DMG / Windows NSIS / Publish 三个 job **全部成功**（run `32155058004`）。

## 4. 验收

- Release `v1.3.0` 已挂：`lebi-AI_1.3.0_aarch64.dmg`、`lebi-AI_1.3.0_x64-setup.exe`、`.app.tar.gz` + `.sig`、`latest.json`（2026-08-18 15:43 UTC）。
- 真机「点更新」由用户在 1.2 → 1.3 上走查（U3 后半）。
- **遗留：** main 分支 CI `cargo test` 在 ubuntu runner 上失败（9f64eed 起即有，非本次引入）；macOS 本地 414 全绿。fmt / check / clippy 已绿。需有仓库管理员权限查该步日志定位。
