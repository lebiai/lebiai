//! Re-export shared companion context builder from `hermes-channel`.
//!
//! Same type as `hermes-gui/src/context.rs` — single implementation, no fork.

pub use hermes_channel::CompanionContextSources as ContextSources;
