# GUI bundle resources

## `markitdown-sidecar/`

Generated by:

```bash
# repo root
scripts/prepare-markitdown-bundle.sh
```

Contains a relocatable MarkItDown install (venv + wrapper). Packed into the
macOS app via `tauri.conf.json` → `bundle.resources`.

Do not commit the venv (see root `.gitignore`). Rebuild on each release machine.
