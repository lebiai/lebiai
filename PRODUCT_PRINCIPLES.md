# lebi-AI（乐彼AI）· 产品原则（P0 权威）

> **版本：** v0.11
> **更新日期：** 2026-08-14
> **效力：** 本仓库**唯一**产品法。与实现、提示词、README、`docs/`、临时方案冲突时，**只改冲突方或先升本文**——禁止另立第二套产品目标。
> **文体：** 本文只陈述**现行**规则。变更对照、修订说明、过程叙事只写在 `docs/records/`。
> **维护：** 变更须改版本号与日期，并**同一次变更内**同步 `AGENTS.md`、`DEVELOPMENT_RULES.md`、检查 `README.md`，且写入 `docs/records/`。未同步 = 升版未完成。
> **下游：** [`docs/`](./docs/) 按种类 B–H 分目录，**不是**权威。体验规格见 [`docs/spec/work-companion-solution.md`](./docs/spec/work-companion-solution.md)，冲突以本文为准。

---

## 产品定义卡

| 项 | 文案 |
|----|------|
| **产品** | 乐彼AI（lebi-AI） |
| **品类** | **工作搭子 AI** · 本地 |
| **Slogan** | **越用越像你的手感** |
| **一句话** | **接得住你的想法，推得动你的事，必要时敢顶你——第二次更准。** |
| **英文钉** | *Feels more like your hand every time. Local. Sharper every yes.* |
| **爱上因为** | ① 被准确理解（含跨次工作）② 建议像为他定制 ③ 对齐他的思想主轴 ④ **有来有回、不讨好**（理解≠赞同；接住→张力→选项→你定）⑤ 同类事第二次更顺 |
| **默认主场** | 谋划 · 表达 · 决策 · 复盘 · 把事推进 |
| **明确不是** | 闲聊玩具 · 讨好机器 · 行业垂直 · **写代码专用**（代码可偶发，不是灵魂）· **生活/情感陪聊主叙事** · **你的搭档/下属**（搭子陪你一起做，不替你分工干活） |
| **关系规则** | 理解 ≠ 赞同；用户主权；本地可审；进化须批准；不取代意志；**搭子，不是搭档**——陪你一起把事做成，不是替你干活 |
| **关系用词** | 中文用户可见：**搭子**。英文钉：**work companion**。禁止当产品主词：搭档、下属、工作伴侣、生活伴侣；禁止用 partner 表示「替你干活的人」 |
| **用户默认路径** | 下载桌面 GUI → 试用期内配置 API Key → **对话** → 批准进化候选 |
| **授权** | 桌面主能力受试用 / 授权门禁：试用 **3 天**（本机首次可用起算）；过期锁定对话等主能力；授权码本机验签，不绑机、无账号 |

任何功能 / 文案 / 提示词须能回答：**强化爱上①～⑤的哪一条？** 答不出 → 不做或降级。
任何文档 / 方案须能回答：**是否是这张定义卡？** 答不出或另起品类 / 口号 / 主路径 → 否决或先改本文。

---

## 第零条：第一性原理（强制推理）

> 不准靠类比做产品。拆到真，再从真往上建。

**类比** = 因为别的产品这样、以前这样、别人这样，所以我们也这样。  
**第一性原理** = 先写下对本问题不可再拆的事实，再从这些事实推出做法。

定义卡与下文各条是对本产品已经拆清的真，以及由真推出的法。新问题仍须走完下面三步，不得只引用条号交差，也不得把定义卡当「可以堆功能」的类比借口。

### 三步（方案必须写全）

1. **拒绝类比**  
   写明：这不是因为某某产品、旧习惯或「大家都这样」。  
   若唯一理由是类比 → **停**。

2. **拆到真**  
   列出不可再拆的事实。对本产品，真必须落在：用户要完成的事、谁说了算、数据在哪、这一步实际发生什么、用户看见什么。  
   不得把未验证的偏好、竞品功能表、口头「最佳实践」写成真。

3. **从真往上建**  
   做法、界面、确认、文档、提示词都是推论。写清从哪一条真推出。  
   推不出 → **不做**。

条款互相冲突时：**第零条覆盖细则**。细则是推论，可以改；本条的推理程序不改。

### 禁止

- 以 ChatGPT / Cursor / 其它 Agent / 律师版 / 通用后台 / 「行业惯例」为设计起点
- 只填场景、不写「拒绝了什么类比 / 拆出的真 / 如何推出」
- 把类比包装成「参考」当唯一依据

---

## 核心理念

乐彼AI 是**本地工作搭子**：懂你怎么想、怎么干活；陪你一起把事做成；有共鸣也有交锋；
经你批准后沉淀标准、情节与技能，因而**越用越贴**。Rust；单一引擎、多入口。

「对话」是**共事界面**，不是闲聊场。成功标准是用户愿意回来、觉得「它懂我还敢跟我较真」，
不是「模型又升级了」。

目标用户：**个人用户与开发者**。

**体感公式（工程层）：**

```
体感 = 共事 Do × 轨迹 Continuity × 关照 Care × 进化 Evolve
```

| 环 | 用户语言 | 工程含义 |
|----|----------|----------|
| **Do** | 事被推进 | 工具、产出、可核对路径 |
| **Continuity** | 你懂我 / 上次类似 | 有锚再认出；无锚不装记得 |
| **Care** | 适合我的意见 | 交付后有分寸打磨；不话痨、不讨好 |
| **Evolve** | 第二次更准 | skill / memory / distill；**批准后**落盘 |

- **好用：** 单一二进制；多入口同数据；主界面称**对话**（禁止用「聊天」作产品主词）；用户可见面**好看、好走**
- **高效：** 高频路径最短；reflection 不挡输入
- **越贴：** 偏好 / 标准 / 工作情节 / 技能在批准后累积

**入口角色：**

| 角色 | 是 | 不是 |
|------|----|------|
| **用户默认路径** | 桌面 GUI | 不是「必须开终端」 |
| **引擎装配 / 调试入口** | CLI（子命令最全） | 不是面向非技术用户的默认路径 |
| **手机 / IM** | 同一引擎的表面；**已接线** | 不是当前默认交付；不得写成主路径，也不得在已接线时写「正在开发」 |
| **`hermes ask`** | 一次性提问 | 不是工作搭子路径（无搭子身份、确认策略不同） |

**授权：** 试用 3 天；过期锁主能力；码在本机验签；购买在站外（微信 iodine001）。不改变「数据本地明文」。交互细节见 [`docs/spec/license-ux.md`](./docs/spec/license-ux.md)（种类 D，服从本文）。

**用户主词 vs 内部术语：** 用户可见中文主词是**对话**、**搭子**。内部协议名（如 command `chat`、WS 路径 `/chat`）可保留技术名；面向用户的文档与 UI 必须说「对话」。

**每一次改动必须产品经理 + 架构师视角。** 产品经理对用户操作与视觉负全责。禁止修修补补。

---

## 第一条：自我进化（Self-Evolution）— 手段，不是对外主词

> 对外主词：**越用越像你的手感 / 第二次更准**。  
> 自我进化是引擎手段，必须**用户可审、可批准、可回滚**。

### 必须
- 会话结束后运行 **full reflection**：把整段对话提炼为 skill / memory / conflict 三类候选
- 反思候选**必须先经用户确认 / 拒绝**，确认后以明文 Markdown 落盘
- **micro-reflection** 在轮次间异步轻量运行（~500 tokens），绝不阻塞输入
- 记忆是**一类事的生效规则**（怎么写、怎么查、怎么交、怎么排），不是会话流水账。同类事只留**一条正在生效的**；用户改主意则新条 `supersedes` 旧条，旧条只留档不注入
- 只从「下次不说仍应照做」的差里蒸馏（否了什么、坚持了什么、定稿相对初稿）。过期状态、工具环境、空壳、用户原话复读 **不得** 落盘或进待审
- `hermes distill` 把字面近重复聚类并融合；**同一格规则变了**必须靠 supersede，不能只靠字面聚类
- 记忆支持 supersedes 链（新记忆取代旧记忆），并可追踪 effectiveness
- **对话工具写入语义（精确化）：**
  - `skill_create` / `skill_install` / `skill_delete`：**必须**用户确认（GUI/CLI 弹窗）
  - 工具路径 **禁止** `always_active=true`（仅 Skills 面板人工开启）
  - `memory_save`：桌面/CLI 默认放行（用户可见工具事件 + 可选 Always Allow）；**IM 白名单不含** 该工具
  - 反思候选与工具直写是两条路径，均不得在无 UI 的 IM 面上静默改写长期状态

### 禁止
- 反思候选未经用户确认自动写入技能 / 记忆（`auto_accept_memories` 默认 false）
- 在 IM 等无确认 UI 表面暴露 durable 写工具
- reflection / micro-reflection 阻塞用户输入与回复
- 知识库只增长不收敛（无 distill / supersedes 机制）
- 把会话摘要、当天心情、探测环境、空模板当作成长期记忆

---

## 第二条：本地优先（Local-First）

> 数据默认只在本机，格式是**明文文件**，用户随时可读、可备份、可进 git。

### 必须
- 无数据库、无消息队列；会话用 JSONL，技能/记忆用 Markdown + YAML frontmatter
- 数据目录 `~/.lebi-ai/`（可用环境变量 `LEBI_DATA_DIR` 覆盖）；项目级 `./.lebi-ai/`
- **产品隔离：** 通用乐彼AI（lebi-AI）与律师版（`~/.lebi-law`）**不得**共用默认数据根；两个产品的 GUI 不得读写同一目录
- 含密钥的配置文件（`config.toml`、`wechat.toml`、`feishu.toml`、`server.token`）权限 **0600**
- 唯一常规出站：用户自备 Key 的 AI API；用户主动触发的 Web 搜索 / MCP 服务器除外

### 禁止
- 把会话 / 记忆 / 技能默认上传到第三方服务
- 明文密钥、把 token 提交进 git
- 引入必须常驻的数据库 / 中间件（单一二进制原则）

---

## 第三条：单一引擎、多入口（One Engine, Many Surfaces）

> CLI、桌面 GUI、手机客户端、IM 渠道共享**同一引擎、同一份数据**。

### 必须
- 所有入口共用 `crates/hermes-core` 定义的核心抽象与 `~/.lebi-ai/` 数据
- GUI / Flutter / IM 渠道只是表面（surface），不复制引擎逻辑
- `hermes-server`（HTTP/WS）是 Flutter 客户端的后端：**GUI 命令的子集 + WebSocket 对话帧**，
  **不是**与 Tauri commands 1:1 镜像（能力矩阵见 [`docs/snapshot/project-map.md`](./docs/snapshot/project-map.md)；种类 H，服从本文）
- **用户默认走 GUI**；CLI 只作为引擎装配 / 调试 / 全量子命令入口；手机与 IM 已接线但非默认交付
- 微信 / 飞书 / Telegram 共享同一套渠道驱动层（`hermes-channel`），只保留协议差异
- 无确认 UI 的表面（IM）必须 **fail-closed**（工具白名单 + 发送方 allowlist）

### 禁止
- 为某个入口 fork 引擎、复制一套「平行逻辑」
- 某个入口独占功能导致数据格式分叉
- 在权威文档中谎称 server ⇄ GUI「1:1」而实现为子集

---

## 第四条：安全默认（Secure by Default）

### 必须
- `hermes serve` 默认只绑 `127.0.0.1`，且**每个** `/api/v1/*` 请求（REST + WS）必须带
  bearer token，无匿名端点；token 不存在时自动生成（32 字节随机 hex，0600）
- **禁止**把完整 token 写入结构化日志；仅允许指纹（前 4…后 4）
- 暴露到公网**必须**经反向代理套 TLS（Caddy / Cloudflare Tunnel 等），
  二进制内不做 TLS、不裸奔公网
- **特别危险**的操作才经 confirm 流程把关，并说明风险原因（如破坏性 shell、
  远程装技能、删记忆/技能、未知 MCP、`skill_create`）；工作区内常态读写与普通 shell **默认放行**。
  鉴权管「谁能连」，confirm 管「特别危险时要不要你点头」；workspace 等硬边界始终生效
- `permissions.allow` **不得**跳过绝对高危检测（rm -rf、sudo、curl|sh、skill_install、MCP 等）
- 无确认通道时，需要确认的工具 **fail-closed（拒绝）**（`hermes ask` 须显式 auto-Allow 通道）
- IM 发送方默认拒绝：须配置 `~/.lebi-ai/channel-allowlist.toml`（`allowed = ["*"]` 才是显式全开）
- 会话 load/delete path 必须落在 `sessions/` 根下
- `web_fetch` 拦截私网 / 元数据地址（SSRF）
- 远程安装技能强制 `always_active=false`，且受配额限制（≤50 文件 / 单文件 ≤100 KB / 总量 ≤5 MB）

### 禁止
- 服务器「裸奔」：无 token、绑 `0.0.0.0` 且明文 HTTP 上公网
- 把完整 token 打印进日志 / 提交进 git
- 无 allowlist 的 IM 机器人对任意陌生人开放

---

## 第五条：变更必须可验收、可追溯

> 任何有用户影响或架构影响的修改，必须完整走完
> **方案 → 实施 → 测试 → 验收**，并写入 `docs/records/`。

- **未验收 = 未完成**，不得对外声称可用
- 验收必须以**用户价值**为准：好用、高效、可进化；用户可见面还必须**好看、好走**。仅编译通过 ≠ 完成
- 过程与结论必须落盘（见 `docs/records/` 与 `DEVELOPMENT_RULES.md`）

---

## 第六条：文档种类（闭环 · 强制）

> 先选种类再写。选不出就不写。禁止发明第九种。

### A · 权威（仅根目录 4 个）

1. `PRODUCT_PRINCIPLES.md`（本文 · P0 · **唯一产品法**）
2. `DEVELOPMENT_RULES.md`（P1 · 开发法，不得新增产品目标）
3. `AGENTS.md`（P2 · 协作入口，不得新增产品目标）
4. `README.md`（P3 · 用户简介，只允许本文的用户语言子集）

### B–H · 下游（全部非权威 · 目录即种类）

| 种 | 名称 | 只回答 | 目录 |
|----|------|--------|------|
| **B** | 用户说明书 | 人怎么装、怎么配 Key、怎么配白名单 | `docs/guide/` |
| **C** | 发布说明 | 这一版用户多得到什么 | `docs/releases/` |
| **D** | 操作规格 | 某一块用户面怎么走、长什么样 | `docs/spec/` |
| **E** | 开发者手册 | 怎么跑 GUI、怎么 serve、怎么发码 | `docs/dev/` |
| **F** | 台账 | 这一次改了什么、验了没有 | `docs/records/` |
| **G** | 探索 | 未立项方向 | `docs/explore/` |
| **H** | 快照 | 某日的地图/学习（会过期） | `docs/snapshot/` |

文首必须写：**种类**、**非权威**、**冲突以 P0 为准**、日期。  
索引：[`docs/README.md`](./docs/README.md)。只列 A–H，不把脚本、签发页、墓碑当正文种类。

台账（F）可分型：产品 / 工程 / 发布。工程修复用短记录，不写成长篇体验文。

### 不是文档（禁止当文档生产、禁止进种类表）

| 代号 | 是什么 | 例子 |
|------|--------|------|
| **R** | 运行时资源 | bundled `SKILL.md` |
| **T** | 工具说明 | `scripts/` 头注释、签发 HTML |
| **X** | 外源 / 模板 | 第三方 Agent 技能、脚手架 README |

### 禁止
- 在根目录新增第五份说明 md
- 选不出 A–H 仍落盘
- 用 B–H **另立**与本文冲突的品类、口号、用户主路径
- 把 H / G 写成现行目标或验收依据
- 方向变更只改 `docs/` 不升本文
- 在 A 类正文写变更对照（对照只写 F）

---

## 第七条：产品经理视角 + 架构师视角（禁止修修补补）

> 修问题 ≠ 打补丁。必须先走第零条，再想清楚「为谁、解决什么、系统应如何正确工作」。
> 产品经理不只写「场景和价值」——**凡用户看得见、摸得着的，操作与视觉由产品经理拍板**（见第十一条）。

### 产品经理视角（必须回答）

1. **用户是谁、场景是什么？**（哪个入口、要完成哪一件事）
2. **用户怎么走完？** 主操作是什么、几步、先看见什么、点哪里、失败时怎么办
3. **看起来怎么样？** 层级、主次、留白、文案；空态 / 加载 / 错误长什么样
4. **是否好走、好看？** 步骤能否再少；主按钮是否一眼可见；是否像调试台
5. **成功标准是什么？** 可观察：走完、好看、不靠人从旁指点
6. **不做什么？**（避免顺手堆功能、堆控件）

答不出 1–5 → **不准开工**。只写「能用 / 能装 / 能导出」而写不出操作与视觉 → **不准开工**。

### 架构师视角（必须回答）

1. **根因**在哪一层？（配置 / 协议 / 状态机 / UI 加载 / 权限…）
2. **正确的长期设计**是什么？是否与引擎共享、与各入口契约一致？
3. **默认路径**是否稳定？（例如：桌面 GUI 必须从 `ui/dist` 加载，不依赖偶然起的 Vite `:5173`；不依赖本机碰巧存在的某路径）
4. **有无副作用？**（双源状态、静默吞错、多实例、契约漂移）
5. **是否按产品经理的操作与视觉规格实现？** 不得自行用「先能跑」的控件堆出用户面

### 明确禁止的「修修补补」

| 禁止 | 说明 |
|------|------|
| 只改表象 | 遮住错误、强行关 loading，却不修资源 / URL 根因 |
| 环境运气依赖 | 依赖本机碰巧开着 5173、碰巧有某路径 |
| 分叉与特例 | 为某一症状再开一套 if/else，而不收敛到单一正确路径 |
| 无方案动刀 | 未写清根因与验收就散落改文件 |
| 补丁叠补丁 | 同一问题多次临时处理却不回看架构与产品路径 |
| 先功能后界面 | 先堆能跑的控件，指望有人再校准美观与操作 |

### 允许的「最小正确修复」

在根因清晰、默认路径正确、**操作与视觉已说清**的前提下，**改动面可以小**，但必须是「正确设计的最小实现」，
不是「能骗过现象的补丁」。

---

## 第八条：技能边界（Bundled / User / Project）

> 技能是「按需加载的专家知识」：发现（索引只含 name + description）→ 激活
> （`skill_read` 读正文）→ 执行（按需读 scripts / references）。禁止把引擎能力伪装成技能。

### 技能范围总表

| 范围 | 位置 | 来源 | 可否删除 |
|------|------|------|----------|
| **内置技能（bundled）** | `~/.lebi-ai/skills/`（自动安装） | 编译进二进制：`memory-palace`（生成）、`skill-creator` / `find-skills`（`include_str!`） | **否**（自动重装，删除无意义） |
| **用户技能（User）** | `~/.lebi-ai/skills/` | 用户自建 / `skill_install` 安装 | 可删除 |
| **项目技能（Project）** | `./.lebi-ai/skills/`（项目根内） | 项目内维护 | 可删除 |

### 必须
- 内置技能与引擎能力（tools / prompt / 权限）**分开维护**：`hermes-tools` 是引擎能力，
  不是可卸载的 SKILL.md
- 索引只暴露 name + description；正文与配套文件按 Progressive Disclosure 按需读取
- 远程安装技能强制 `always_active=false`；配额与路径校验必须保留
- `delete_skill` / 安装覆盖对内置技能名一律**拒绝**（它们启动即重装，明示错误避免困惑）

### 禁止
- 把 tools / 安全策略 / 系统提示改成可卸载的 SKILL.md
- 未经检查的路径穿越（`..`、绝对路径、超深度、保留名）进入技能安装
- 让远程技能默认注入每个系统提示（always_active 必须显式关闭）

---

## 第九条：代码高效、无冗余、改完即清旧（强制）

> **所有代码必须高效、无冗余。**
> **每一次更新或修改，必须及时清理被替换的旧实现、死代码、失效注释与过时文档。**
> **凡是旧的、已不再走的路径，都必须清掉——禁止「新路径堆上、旧路径留尸」。**

### 必须

| 要求 | 含义 |
|------|------|
| **高效** | 热路径少无功：无多余 I/O、无重复解析、无无意义重试/sleep、无双写状态 |
| **无冗余** | 同一职责单一实现；禁止复制粘贴第二套平行逻辑「以后再删」 |
| **改完即清** | 新默认路径落地后，**同一次变更内**删除旧代码 / 旧分支 / 旧配置 / 旧入口 |
| **注释诚实** | 删除过时注释、注释掉的大段代码；注释只解释「为什么」，不留考古层 |
| **契约同步** | 改 API / 工具名 / UI 入口时，同步删调用方、i18n 废 key、权限白名单废项 |

### 禁止

| 禁止 | 说明 |
|------|------|
| 双轨并存当常态 | 「新旧两套都留着，先开 flag」而无删除计划与期限 |
| 注释掉的尸块 | 大段旧实现当备份；版本库已有历史 |
| 死代码 / 死导出 | 未引用的 fn / 模块 / 组件 / 文案 key 留在树上 |
| 文档撒谎 | README / 提示词仍写已删除路径或旧入口 |
| 叠床架屋 | 为兼容一次临时行为永久留下特判，又不收敛 |

### 与第七条关系

- 第七条禁的是**错误的补丁思维**。
- 本条禁的是**正确改完后仍堆垃圾**。
- 两者同时违反 → 变更未完成。

细则：[`DEVELOPMENT_RULES.md`](./DEVELOPMENT_RULES.md) §六「代码卫生」、质量门槛第 8 项。

---

## 第十条：权威唯一、禁止偏移（强制）

> 仓库里只有一套产品法。一切实现、文案、提示词、`docs/`、台账必须站在本文之上。

### 必须
- 产品定义、关系、入口角色、授权、默认值**只在本文**立法；下游只引用，不改写
- 身份协议唯一源码：`crates/hermes-core/src/companion.rs`。必须与本文定义卡**同词**（搭子 / work companion）。改性格先改本文再改该文件
- 发现偏移：改偏移方；若产品本身变了，**先升本文**再改下游。禁止只改蓝图 / 规格 / README
- P1 / AGENTS / README 文首必须写明对齐的 **P0 版本号**；对不齐 = 未完成
- 新 `docs/` 文必须先选种类 B–H，放入对应目录，文首写种类与「冲突以 P0 为准」
- **权威正文只写现行规则。** 修订说明、对照、过程叙事只属于 `docs/records/`

### 禁止
- 平行产品愿景（第二套口号、品类、主路径、关系模型）
- 用「完整方案 / 冻结规格 / 学习报告」覆盖本文
- P1 / AGENTS / README 与本文版本号不一致
- 已接线表面写成「正在开发」；非默认表面写成用户主路径
- 用「搭档 / 工作伴侣 / 生活伴侣」作产品主词；把「陪伴」写成生活/情感主叙事
- 在 P0 / P1 / AGENTS / README 写变更对照（例如过程叙事、修订对照）。对照只写台账

---

## 第十一条：产品经理对操作与视觉负责（强制）

> 用户默认路径是桌面 GUI。产品经理对**用户看得见、点得着的每一步**负责：好不好走、好不好看。
> 架构师保证引擎正确、默认路径稳定。开发者按规格实现。三者缺一不可。

### 职责

| 角色 | 负责 |
|------|------|
| **产品经理** | 操作路径、信息层级、视觉、文案、空/载/错态、确认是否该出现。用户面未说清 = 方案未完成 |
| **架构师** | 根因、共享引擎、安全、默认加载路径。按产品经理的操作与视觉规格落地，不得用调试控件顶替用户面 |
| **开发者** | 正确实现；用户可见面不得自行发明一套「先能跑」的布局 |

协作者 / Agent 代行产品经理时，**必须真正履行上表**，不得只填场景交差。

### 操作（好走）

- 高频路径步骤最少；主操作一个、位置稳定、一眼能找
- 次要操作退后（菜单、次按钮、折叠），不与主操作抢
- 特别危险才打断；常态读写不弹窗骚扰
- 空态说明下一步；错误说明怎么恢复；加载有结束感
- 用户不读说明书、不靠人在旁边指点，能走完默认路径

### 视觉（好看）

- 有层级：标题 / 正文 / 辅助信息字号与颜色分开
- 有留白：不堆满、不对齐混乱、不一条线贴死边缘
- 与已有 GUI 视觉系统一致（色、圆角、间距、动效克制）
- 控件像产品，不像后台或调试台
- 文案短、人话、主词是「对话」「搭子」

### 必须
- 方案里写清：用户先看见什么、点什么、成功长什么样、失败长什么样
- 用户可见面的验收含**操作走查**与**视觉走查**（按真实使用点完；不只看截图）
- 涉及布局时同时考虑窄窗（与已有最小窗口约束一致）

### 禁止
- 「先做通再调好看」
- 用未设计的堆砌交差（无层级的表单墙、无空态的空白页、原始错误当用户文案）
- 功能正确但难看、难找、难完成——仍算**未完成**
- 把操作与视觉推给事后校准

---

## 决策检查清单

0. [ ] **第一性原理：** 拒绝了什么类比？拆出的真是什么？做法如何从真推出？写不出 → 不准开工
1. [ ] 用户需要额外安装什么吗？（答案须为：无——单一二进制 / 编译产物）
2. [ ] 非技术用户能完成吗？（默认路径是 **GUI**，不强制终端）
3. [ ] 对用户是否**更好用、更高效、更能进化**？（答不上来 → 不做）
4. [ ] 数据能否继续留在本机明文文件？（默认须为：能）
5. [ ] 除 AI API（与用户主动触发的 Web/MCP）外数据会离开用户电脑吗？（答案须为：否）
6. [ ] 引擎是否被所有入口共享？有没有为某入口 fork 平行逻辑？
7. [ ] 是否已有 / 将写 `docs/records/` 变更记录并完成验收？
8. [ ] 新增说明是否先选 A–H、放入对应目录、文首已标明种类、且未另立产品目标？
9. [ ] **产品经理：** 操作怎么走、看起来怎么样、空/载/错态是否写清？是否好看、好走？
10. [ ] **架构师：** 根因与长期正确默认路径是否写清？是否按操作与视觉规格实现？
11. [ ] **技能边界：** 改动落在 ①引擎 / ②bundled / ③user / ④project 哪一层？是否把引擎能力写成可卸载技能？
12. [ ] **安全：** server 是否默认本机 + token？公网是否必须 TLS？密钥是否 0600？
13. [ ] **代码卫生：** 是否高效、无冗余？旧实现 / 死代码 / 过时注释与文案是否已在本变更内清理？
14. [ ] **权威：** 是否未另立口号/品类/主路径？P1/AGENTS/README 是否对齐本文版本号？权威正文是否只写现行规则？
15. [ ] **授权：** 是否为试用 + 本机验签、无账号？是否未把授权写成云登录？
16. [ ] **用词：** 用户可见是否为「对话」「搭子」？身份协议是否与定义卡同词？

任一不满足 → 否决或改方案。

---

## 文档位置总表

| 文件/目录 | 层级 |
|-----------|------|
| `PRODUCT_PRINCIPLES.md` | **P0 唯一产品法**（本文） |
| `DEVELOPMENT_RULES.md` | P1 开发法（服从 P0） |
| `AGENTS.md` | P2 执行入口（服从 P0） |
| `README.md` | P3 用户简介（P0 子集） |
| `docs/guide/` | B 用户说明书 |
| `docs/releases/` | C 发布说明 |
| `docs/spec/` | D 操作规格 |
| `docs/dev/` | E 开发者手册 |
| `docs/records/` | F 台账（历史 ≠ 现行法） |
| `docs/explore/` | G 探索 |
| `docs/snapshot/` | H 快照 |
| `crates/hermes-core/src/companion.rs` | 身份协议**唯一源码**（必须与本文同词） |
