# lebi-AI（乐彼AI）· 产品原则（P0 权威）

> **版本：** v0.5
> **更新日期：** 2026-08-07
> **效力：** 本仓库最高产品约束。与实现、README、临时方案冲突时，以本文为准。
> **维护：** 变更须改版本号与日期，并同步 `AGENTS.md`、`DEVELOPMENT_RULES.md`，且写入 `docs/records/`。
> **完整蓝图：** [`docs/work-companion-solution.md`](./docs/work-companion-solution.md)（体验/本体/协议/验收矩阵；非第二权威，冲突以本文为准）。

---

## 产品定义卡

| 项 | 文案 |
|----|------|
| **产品** | 乐彼AI（lebi-AI） |
| **品类** | **工作搭子 AI** · 本地 |
| **Slogan** | **越用越像你的手感** |
| **一句话** | **接得住你的想法，推得动你的事，必要时敢顶你——第二次更准。** |
| **英文钉** | *Feels more like your hand every time. Local. Sharper every yes.* |
| **爱上因为** | ① 被准确理解（含跨次工作）② 建议像为他定制 ③ 对齐他的思想主轴 ④ **有来有回、不讨好**（理解≠赞同；接住→张力→选项→你定）⑤ 同类事第二次更顺 |
| **默认主场** | 谋划 · 表达 · 决策 · 复盘 · 把事推进 |
| **明确不是** | 闲聊玩具 · 讨好机器 · 行业垂直 · **写代码专用**（代码可偶发，不是灵魂）· **生活/情感陪聊主叙事** · **你的搭档/下属**（搭子陪你一起做，不替你分工干活） |
| **关系规则** | 理解 ≠ 赞同；用户主权；本地可审；进化须批准；不取代意志；**搭子，不是搭档**——陪你一起把事做成，不是替你干活 |

任何功能 / 文案 / 提示词须能回答：**强化爱上①～⑤的哪一条？** 答不出 → 不做或降级。

---

## 核心理念

乐彼AI 是**本地工作搭子**：懂你怎么想、怎么干活；陪你一起把事做成；有共鸣也有交锋；
经你批准后沉淀标准、情节与技能，因而**越用越贴**。Rust；单一引擎、多入口。

「对话」是**共事界面**，不是闲聊场。成功标准是用户愿意回来、觉得「它懂我还敢跟我较真」，
不是「模型又升级了」。

目标用户：**个人用户与开发者**（非商业场景，见 [LICENSE](./LICENSE) PolyForm Noncommercial）。

**体感公式（工程层）：**

```
体感 = 共事 Do × 轨迹 Continuity × 关照 Care × 进化 Evolve
```

| 环 | 用户语言 | 工程含义 |
|----|----------|----------|
| **Do** | 事被推进 | 工具、产出、可核对路径 |
| **Continuity** | 你懂我 / 上次类似 | 有锚再认出；无锚不装记得 |
| **Care** | 适合我的意见 | 交付后有分寸打磨；不话痨、不讨好 |
| **Evolve** | 第二次更准 | skill / memory / distill；**批准后**落盘 |

- **好用：** 单一二进制；多入口同数据；主界面称**对话**
- **高效：** 高频路径最短；reflection 不挡输入
- **越贴：** 偏好 / 标准 / 工作情节 / 技能在批准后累积

**每一次改动必须产品经理 + 架构师视角。** 禁止修修补补。

---

## 第一条：自我进化（Self-Evolution）— 手段，不是对外主词

> 对外主词：**越用越像你的手感 / 第二次更准**。  
> 自我进化是引擎手段，必须**用户可审、可批准、可回滚**。

### 必须
- 会话结束后运行 **full reflection**：把整段对话提炼为 skill / memory / conflict 三类候选
- 候选**必须先经用户确认 / 拒绝**，确认后以明文 Markdown 落盘
- **micro-reflection** 在轮次间异步轻量运行（~500 tokens），绝不阻塞输入
- `hermes distill` 把近似重复记忆聚类并融合，让知识库**收敛**而非只增长
- 记忆支持 supersedes 链（新记忆取代旧记忆），并可追踪 effectiveness

### 禁止
- 未经用户确认自动写入技能 / 记忆
- reflection / micro-reflection 阻塞用户输入与回复
- 知识库只增长不收敛（无 distill / supersedes 机制）

---

## 第二条：本地优先（Local-First）

> 数据默认只在本机，格式是**明文文件**，用户随时可读、可备份、可进 git。

### 必须
- 无数据库、无消息队列；会话用 JSONL，技能/记忆用 Markdown + YAML frontmatter
- 数据目录 `~/.lebi-ai/`（可用环境变量 `LEBI_DATA_DIR` 覆盖）；项目级 `./.lebi-ai/`
- **产品隔离：** 通用乐彼AI（lebi-AI）与律师版（`~/.lebi-law`）**不得**共用默认数据根；禁止再让两个 GUI 读写同一目录
- 含密钥的配置文件（`config.toml`、`wechat.toml`、`feishu.toml`、`server.token`）权限 **0600**
- 唯一常规出站：用户自备 Key 的 AI API；用户主动触发的 Web 搜索 / MCP 服务器除外

### 禁止
- 把会话 / 记忆 / 技能默认上传到第三方服务
- 明文密钥、把 token 提交进 git
- 引入必须常驻的数据库 / 中间件（单一二进制原则）

---

## 第三条：单一引擎、多入口（One Engine, Many Surfaces）

> CLI、桌面 GUI、手机客户端、IM 渠道共享**同一引擎、同一份数据**。

### 必须
- 所有入口共用 `crates/hermes-core` 定义的核心抽象与 `~/.lebi-ai/` 数据
- GUI / Flutter / IM 渠道只是表面（surface），不复制引擎逻辑
- `hermes-server`（HTTP/WS）是 Flutter 客户端的后端，路由与 Tauri commands **1:1 对应**
- 微信 / 飞书 / Telegram 共享同一套渠道驱动层（`channel.rs`），只保留协议差异

### 禁止
- 为某个入口 fork 引擎、复制一套「平行逻辑」
- 某个入口独占功能导致数据格式分叉
- server 与 GUI 的 AppState / DTO 各自漂移（契约必须一致）

---

## 第四条：安全默认（Secure by Default）

### 必须
- `hermes serve` 默认只绑 `127.0.0.1`，且**每个** `/api/v1/*` 请求（REST + WS）必须带
  bearer token，无匿名端点；token 不存在时自动生成（32 字节随机 hex，0600）
- 暴露到公网**必须**经反向代理套 TLS（Caddy / Cloudflare Tunnel 等），
  二进制内不做 TLS、不裸奔公网
- **特别危险**的操作才经 confirm 流程把关，并说明风险原因（如破坏性 shell、
  远程装技能、删记忆/技能、未知 MCP）；工作区内常态读写与普通 shell **默认放行**。
  鉴权管「谁能连」，confirm 管「特别危险时要不要你点头」；workspace 等硬边界始终生效
- 远程安装技能强制 `always_active=false`，且受配额限制（≤50 文件 / 单文件 ≤100 KB / 总量 ≤5 MB）

### 禁止
- 服务器「裸奔」：无 token、绑 `0.0.0.0` 且明文 HTTP 上公网
- 把 token 打印进日志之外的地方 / 提交进 git

---

## 第五条：变更必须可验收、可追溯

> 任何有用户影响或架构影响的修改，必须完整走完
> **方案 → 实施 → 测试 → 验收**，并写入 `docs/records/`。

- **未验收 = 未完成**，不得对外声称可用
- 验收必须以**用户价值**为准：好用、高效、可进化，而非「仅编译通过」
- 过程与结论必须落盘（见 `docs/records/` 与 `DEVELOPMENT_RULES.md`）

---

## 第六条：文档位置（防淹没）

> **仓库根目录仅允许权威文档。其他说明一律进入 `docs/`。**

### 根目录允许的 Markdown（仅 4 个）

1. `PRODUCT_PRINCIPLES.md`（本文 · P0）
2. `DEVELOPMENT_RULES.md`（P1）
3. `AGENTS.md`（P2）
4. `README.md`（P3）

### 禁止
- 在根目录新增第五份及以后的 `*.md` 说明文档
- 用散落的 md 另立与 P0 冲突的产品叙事

### 其他文档
- 一律放在 `docs/`（含变更台账 `docs/records/`）
- 索引见 `docs/README.md`

非「说明文档」例外：`LICENSE`；代码内嵌运行时资源（如 `crates/hermes-cli/src/skills/**/SKILL.md`）。

---

## 第七条：产品经理视角 + 架构师视角（禁止修修补补）

> 修问题 ≠ 打补丁。必须先想清楚「为谁、解决什么、系统应如何正确工作」。

### 产品经理视角（必须回答）

1. **用户是谁、场景是什么？**（用户在哪个入口、哪一步受挫）
2. **修好后用户路径如何变化？**（改前 → 改后，步骤是否更少更清晰）
3. **成功标准是什么？**（可观察：能安装、能对话、能确认候选、能导出…）
4. **不做什么？**（避免顺手堆功能）

答不出 1–3 → **不准开工**。

### 架构师视角（必须回答）

1. **根因**在哪一层？（配置 / 协议 / 状态机 / UI 加载 / 权限…）
2. **正确的长期设计**是什么？是否与引擎共享、与各入口契约一致？
3. **默认路径**是否稳定？（例如：桌面 GUI 必须从 `ui/dist` 加载，不依赖偶然起的 Vite `:5173`；不依赖本机碰巧存在的某路径）
4. **有无副作用**？（双源状态、静默吞错、多实例、契约漂移）

### 明确禁止的「修修补补」

| 禁止 | 说明 |
|------|------|
| 只改表象 | 遮住错误、强行关 loading，却不修资源 / URL 根因 |
| 环境运气依赖 | 依赖本机碰巧开着 5173、碰巧有某路径 |
| 分叉与特例 | 为某一症状再开一套 if/else，而不收敛到单一正确路径 |
| 无方案动刀 | 未写清根因与验收就散落改文件 |
| 补丁叠补丁 | 同一问题多次临时处理却不回看架构与产品路径 |

### 允许的「最小正确修复」

在根因清晰、默认路径正确的前提下，**改动面可以小**，但必须是「正确设计的最小实现」，
不是「能骗过现象的补丁」。

---

## 第八条：技能边界（Bundled / User / Project）

> 技能是「按需加载的专家知识」：发现（索引只含 name + description）→ 激活
> （`skill_read` 读正文）→ 执行（按需读 scripts / references）。禁止把引擎能力伪装成技能。

### 技能范围总表

| 范围 | 位置 | 来源 | 可否删除 |
|------|------|------|----------|
| **内置技能（bundled）** | `~/.lebi-ai/skills/`（自动安装） | 编译进二进制：`memory-palace`（生成）、`skill-creator` / `find-skills`（`include_str!`） | **否**（自动重装，删除无意义） |
| **用户技能（User）** | `~/.lebi-ai/skills/` | 用户自建 / `skill_install` 安装 | 可删除 |
| **项目技能（Project）** | `./.lebi-ai/skills/`（项目根内） | 项目内维护 | 可删除 |

### 必须
- 内置技能与引擎能力（tools / prompt / 权限）**分开维护**：`hermes-tools` 是引擎能力，
  不是可卸载的 SKILL.md
- 索引只暴露 name + description；正文与配套文件按 Progressive Disclosure 按需读取
- 远程安装技能强制 `always_active=false`；配额与路径校验必须保留
- `delete_skill` / 安装覆盖对内置技能名一律**拒绝**（它们启动即重装，明示错误避免困惑）

### 禁止
- 把 tools / 安全策略 / 系统提示改成可卸载的 SKILL.md
- 未经检查的路径穿越（`..`、绝对路径、超深度、保留名）进入技能安装
- 让远程技能默认注入每个系统提示（always_active 必须显式关闭）

---

## 第九条：代码高效、无冗余、改完即清旧（强制）

> **所有代码必须高效、无冗余。**
> **每一次更新或修改，必须及时清理被替换的旧实现、死代码、失效注释与过时文档。**
> **凡是旧的、已不再走的路径，都必须清掉——禁止「新路径堆上、旧路径留尸」。**

### 必须

| 要求 | 含义 |
|------|------|
| **高效** | 热路径少无功：无多余 I/O、无重复解析、无无意义重试/sleep、无双写状态 |
| **无冗余** | 同一职责单一实现；禁止复制粘贴第二套平行逻辑「以后再删」 |
| **改完即清** | 新默认路径落地后，**同一次变更内**删除旧代码 / 旧分支 / 旧配置 / 旧入口 |
| **注释诚实** | 删除过时注释、注释掉的大段代码；注释只解释「为什么」，不留考古层 |
| **契约同步** | 改 API / 工具名 / UI 入口时，同步删调用方、i18n 废 key、权限白名单废项 |

### 禁止

| 禁止 | 说明 |
|------|------|
| 双轨并存当常态 | 「新旧两套都留着，先开 flag」而无删除计划与期限 |
| 注释掉的尸块 | 大段旧实现当备份；版本库已有历史 |
| 死代码 / 死导出 | 未引用的 fn / 模块 / 组件 / 文案 key 留在树上 |
| 文档撒谎 | README / 提示词仍写已删除路径或旧入口 |
| 叠床架屋 | 为兼容一次临时行为永久留下特判，又不收敛 |

### 与第七条关系

- 第七条禁的是**错误的补丁思维**。
- 本条禁的是**正确改完后仍堆垃圾**。
- 两者同时违反 → 变更未完成。

细则：[`DEVELOPMENT_RULES.md`](./DEVELOPMENT_RULES.md) §六「代码卫生」、质量门槛第 8 项。

---

## 决策检查清单

1. [ ] 用户需要额外安装什么吗？（答案须为：无——单一二进制 / 编译产物）
2. [ ] 非技术用户能完成吗？（GUI / 手机端场景）
3. [ ] 对用户是否**更好用、更高效、更能进化**？（答不上来 → 不做）
4. [ ] 数据能否继续留在本机明文文件？（默认须为：能）
5. [ ] 除 AI API（与用户主动触发的 Web/MCP）外数据会离开用户电脑吗？（答案须为：否）
6. [ ] 引擎是否仍被所有入口共享？有没有为某入口 fork 平行逻辑？
7. [ ] 是否已有 / 将写 `docs/records/` 变更记录并完成验收？
8. [ ] 新增说明是否放在 `docs/` 而非仓库根？
9. [ ] **产品经理：** 用户路径与成功标准是否写清？
10. [ ] **架构师：** 根因与长期正确默认路径是否写清？是否避免修修补补？
11. [ ] **技能边界：** 改动落在 bundled / user / project 哪一层？是否把引擎能力写成可卸载技能？
12. [ ] **安全：** server 是否仍默认本机 + token？公网是否必须 TLS？密钥是否 0600？
13. [ ] **代码卫生：** 是否高效、无冗余？旧实现 / 死代码 / 过时注释与文案是否已在本变更内清理？

任一不满足 → 否决或改方案。

---

## 文档位置总表

| 文件/目录 | 层级 |
|-----------|------|
| `PRODUCT_PRINCIPLES.md` | P0 产品法（本文） |
| `DEVELOPMENT_RULES.md` | P1 开发法 + 变更流程 |
| `AGENTS.md` | P2 执行入口 |
| `README.md` | P3 简介 |
| `docs/` | **唯一**非权威文档区 |
| `docs/records/` | 变更与验收台账 |
